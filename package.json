{
  "name": "appaiam-backend",
  "version": "1.0.0",
  "description": "Backend for Appaiam website",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "echo 'Backend build complete'",
    "test": "echo 'No tests specified'",
    "deploy:railway": "railway up",
    "deploy:render": "echo 'Push to main branch to deploy on Render'",
    "logs:railway": "railway logs",
    "logs:render": "echo 'Check Render dashboard for logs'"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "nodemailer": "^6.9.7",
    "mongoose": "^7.5.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
